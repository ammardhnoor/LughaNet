This project aims to find question semantic similarity in the Arabic language. The sense representation between the LughaNet knowledge base((New Arabic WordNet) and dataset is based on part of speech. 
To measure the similarity between two concepts, the short path method is used.

#first the files in  (project_LUGHNET) folder contian the python codes to constrcuted LUGHNET
#second the (evlaute coverage ontology) to measue the the coverage of LUGHNET in arabic dictionary

#python code (test-question-similarity) to indy the semalirity using  LUGHNET



